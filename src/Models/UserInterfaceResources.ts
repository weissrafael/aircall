export enum TabsEnum {
  inbox = 'inbox',
  archived = 'archived',
  allCalls = 'allCalls',
}

export enum VariantEnum {
  primary = 'primary',
  secondary = 'secondary',
}

export type Variant = 'primary' | 'secondary';
